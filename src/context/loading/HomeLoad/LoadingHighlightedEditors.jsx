
import React from 'react';

const LoadingHighlightedEditors = ({ width, height }) => {
  return (
    <div
      style={{
        backgroundImage:'url(/compressed-background.png)',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
      }}
    >
    </div>
  );
};

export default LoadingHighlightedEditors;
